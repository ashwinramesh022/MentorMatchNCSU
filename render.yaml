services:
  - type: web
    name: mentor-match-backend
    env: python
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    workingDir: backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

